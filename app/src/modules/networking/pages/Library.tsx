/**
 * Re-export the existing Library page as a module route page.
 * The Library page uses a named export, so we re-export it as default
 * for the module loader.
 */
export { Library as default } from '@/pages/Library'
