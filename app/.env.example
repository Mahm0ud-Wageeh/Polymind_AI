# Polymind frontend environment
#
# Point this at your running backend to use the real API + AI streaming.
# Leave VITE_API_URL empty (or set VITE_USE_MOCK=true) to run fully on mocks.
VITE_API_URL=http://localhost:8000/api/v1
VITE_USE_MOCK=false
