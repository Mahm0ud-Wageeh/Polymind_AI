<?php

use Illuminate\Support\Facades\Schedule;

// Example scheduled tasks (Phase 6 — Scheduler).
// Schedule::command('telescope:prune')->daily();
// Schedule::command('auth:clear-resets')->everyFifteenMinutes();
